apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infrastructure-umbrella.fullname" . }}-test-connection"
  labels:
    {{- include "infrastructure-umbrella.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infrastructure-umbrella.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
