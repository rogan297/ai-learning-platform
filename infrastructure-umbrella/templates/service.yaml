apiVersion: v1
kind: Service
metadata:
  name: {{ include "infrastructure-umbrella.fullname" . }}
  labels:
    {{- include "infrastructure-umbrella.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infrastructure-umbrella.selectorLabels" . | nindent 4 }}
